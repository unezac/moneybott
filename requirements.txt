# ─── ICT/SMC + ML Trading System — Python Dependencies ───────────────────────
#
# Core (required)
yfinance>=0.2.36
pandas>=2.0.0
numpy>=1.26.0
scikit-learn>=1.4.0
joblib>=1.3.0

# ML (optional — Agent 3)
xgboost>=2.0.0           # optional; RandomForest used if absent

# NLP / Sentiment (optional — Agent 2)
transformers>=4.40.0     # FinBERT; falls back to keyword rules if absent
torch>=2.2.0             # required by transformers

# Web scraping (optional — Agent 2)
playwright>=1.43.0       # headless browser scraper; falls back to mock data
# After install run:  playwright install chromium

# Utilities
requests>=2.31.0

# Execution (Agent 5) — MetaTrader 5
MetaTrader5>=5.0.45       # Windows only; MT5 terminal must be installed & running

# Backend (FastAPI / Server)
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
